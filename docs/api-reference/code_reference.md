# Python Module Reference

This section provides an auto-generated API reference of the MedNexus AI source files, compiled directly from the code docstrings using `mkdocstrings`.

---

## Flask Web Application (`app.py`)

::: app
    options:
      filters:
        - "^[a-zA-Z]"

---

## Dermatology Logic Engine (`dermatologist_logic.py`)

::: dermatologist_logic
    options:
      filters:
        - "^[a-zA-Z]"

---

## ENT Logic Engine (`ent_logic.py`)

::: ent_logic
    options:
      filters:
        - "^[a-zA-Z]"

---

## Psychiatry Logic Engine (`psych_logic.py`)

::: psych_logic
    options:
      filters:
        - "^[a-zA-Z]"

---

## Pharmacy Logic Engine (`pharmacy_logic.py`)

::: pharmacy_logic
    options:
      filters:
        - "^[a-zA-Z]"

---

## General Medicine Logic (`general_logic.py`)

::: general_logic
    options:
      filters:
        - "^[a-zA-Z]"
